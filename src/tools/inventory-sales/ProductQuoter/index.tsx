'use client'
import type { ToolProps } from '@/tools/types'

export function ProductQuoter({ workspaceId, config }: ToolProps) {
  return (
    <div className="p-4">
      <p className="text-sm" style={{ color: 'var(--text-tertiary)' }}>
        Cotizador — stub (migración en Fase 2)
      </p>
    </div>
  )
}
