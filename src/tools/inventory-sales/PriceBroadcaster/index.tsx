'use client'
import type { ToolProps } from '@/tools/types'

export function PriceBroadcaster({ workspaceId, config }: ToolProps) {
  return (
    <div className="p-4">
      <p className="text-sm" style={{ color: 'var(--text-tertiary)' }}>
        Broadcast de precios — stub (migración en Fase 2)
      </p>
    </div>
  )
}
